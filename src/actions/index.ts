import { blogs } from "./dashboard/blogs";
import { users } from "./dashboard/users";
import { organizations } from "./dashboard/organization";

export const server = {
  blogs,
  users,
  organizations,
};
