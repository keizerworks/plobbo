{
  "name": "plobbo",
  "version": "1.0.0",
  "scripts": {
    "db:push": "pnpm sst shell --target plobbo-drizzle-studio -- pnpm --filter=core db:push",
    "db:generate": "pnpm sst shell --target plobbo-drizzle-studio -- pnpm --filter=core db:generate",
    "db:migrate": "pnpm sst shell --target plobbo-drizzle-studio -- pnpm --filter=core db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250204.0",
    "@plobbo/eslint-config": "workspace:*",
    "@plobbo/tsconfig": "workspace:*"
  },
  "dependencies": {
    "sst": "3.8.3"
  }
}
